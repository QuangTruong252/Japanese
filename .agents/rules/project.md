---
trigger: always_on
description: Quy ước chung cho dự án MaiPace
---

@../../AGENTS.md

Đây là entrypoint Antigravity, không phải bản sao quy ước. Nếu import không được
mở rộng, đọc `AGENTS.md` tại root trước khi làm việc. Mở workspace tại root repo.
Đọc `PRODUCT.md`, mục trạng thái trong `docs/specs/README.md` và chỉ spec/handoff
liên quan theo hướng dẫn chung. Không coi lịch sử chat là nguồn duy nhất.
